import { ColorModifyPipe } from './color-modify.pipe';

describe('ColorModifyPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorModifyPipe();
    expect(pipe).toBeTruthy();
  });
});
