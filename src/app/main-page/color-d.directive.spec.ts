import { ColorDDirective } from './color-d.directive';

describe('ColorDDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorDDirective();
    expect(directive).toBeTruthy();
  });
});
